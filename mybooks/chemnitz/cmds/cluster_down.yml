- name: Galera - Cluster stoppen (Joiner zuerst)
  hosts: chem3,chem2
  become: true
  serial: 1
  tasks:
    - name: Compose down
      command: docker compose -f /docker/galera/docker-compose.yml down

- name: Galera - Bootstrap Node stoppen
  hosts: chem1
  become: true
  tasks:
    - name: Compose down
      command: docker compose -f /docker/galera/docker-compose.yml down
